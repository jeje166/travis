var assert = require('chai').assert;

describe('Testing the unit test functionality', function () {
  it('should be able to assert 1 + 1 = 2', function () {
    assert.equal(1 + 1, 2, 'equals');
  });
});
